export default defineNuxtRouteMiddleware((to, from) => {
    // if (to.params.id === '1') {
    //   throw createError({ statusCode: 404, statusMessage: 'Page Not Found' })
    // }
    console.log('leicj-------------',to, from);
  })
  